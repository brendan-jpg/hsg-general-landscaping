export interface BeforeAfterGroup {
  before_urls: string[];
  after_urls: string[];
}

export interface ServiceProject {
  title: string;
  summary: string;
  photo_urls: string[];
  service_title?: string;
}
